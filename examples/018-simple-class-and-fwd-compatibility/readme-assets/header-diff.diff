--- 010-simple-class/pub/example.h
+++ 018-simple-class-and-fwd-compatibility/pub/example.h
@@ -1,16 +1,22 @@
 #pragma once
 
 //! Contains example definitions to explain cib's functioning
 namespace Example
 {
   //! A vividly trivial class
   //! Contains just a simple method.
   class A
   {
   public:
     A();
     //! Doesn't do anything meaningful
     //! @note It is just for explaining how cib works.
-    int SomeFunc() { return 1; }
+    int SomeFunc() { return 2; }
+
+    //! Adde method in new version
+    int AnotherFunction() { return 109; }
+  private:
+    //! Make sure object layout is changed
+    float x, y;
   };
 }
