## Interface Detection and ABI Stability

This example is like other ABI stability examples to ensure that ABI stability must be ensured even when virtual table of difficult to detect interface classes are disrupted.

